# PRD — 장기요양보험 급여 대시보드

> 2010~2024년 장기요양보험 공단부담금을 지역·시간·서비스 구조 세 축으로 보여주는 인터랙티브 대시보드.

## 1. 배경과 목적

장기요양보험 급여는 2010년 3.2조 원에서 2024년 14.8조 원으로 **4.7배** 커졌다. 규모는 알려져 있지만, 그 돈이 **어느 지역에 어떤 형태로** 쓰이는지는 잘 정리되어 있지 않다.

이 대시보드는 공개 통계 하나를 끝까지 파고들어, 숫자 뒤의 구조 변화를 보여준다.

**성공 기준**: 처음 보는 사람이 3분 안에 "장기요양 급여는 커졌고, 지역차가 있고, 재가 중심으로 바뀌었다"는 세 가지를 이해하고 나간다.

## 2. 페르소나와 사용 맥락

**주 사용자: 데이터 분석가 포트폴리오의 독자** — 채용 담당자, 현업 데이터 리더.

이 독자는 차트가 예쁜지보다 **분석가가 데이터를 얼마나 정확하게 다뤘는지**를 본다. 따라서:

- 지표 정의와 단위를 화면에서 바로 확인할 수 있어야 한다
- 데이터의 한계를 숨기지 않고 명시해야 한다 — 감점 요인이 아니라 가점 요인이다
- 인과관계를 주장하지 않는다. "늘었다"는 말하되 "정책 때문에 늘었다"는 말하지 않는다

**비목표**: 정책 효과 검증, 시군구 단위 분석, 예측.

## 3. 핵심 질문 (= 화면 구성)

| # | 질문 | 섹션 |
|---|---|---|
| Q1 | 어느 지역이 많이 쓰고 적게 쓰나? | 지역 격차 |
| Q2 | 15년 동안 얼마나, 어떤 속도로 커졌나? | 시간의 변화 |
| Q3 | 서비스 구조는 어떻게 바뀌었나? | 구조 전환 |

## 4. 데이터 명세

### 파일
`data.json` (69KB) — `prepare_data.py`가 원본 엑셀에서 생성. 브라우저는 이 JSON만 읽는다.

### 구조

```
{
  meta:  { source, period:[2010,2024], priceBase, unitNote, regionCount:17 }
  grid:  { "서울특별시": {col:2,row:1}, ... }     // 카토그램 좌표
  panel: [ {region, short, year, ...지표}, ... ]  // 253행
  mix:   [ {year, home, inst, severe, mild, cognitive}, ... ]  // 전국 구성비 15행
}
```

### panel 필드

| 필드 | 뜻 | 단위 |
|---|---|---|
| `region` / `short` | 시도명 / 축약명 | — |
| `year` | 연도 | 2010~2024 |
| `total` | 공단부담금 총액 | **억원** (2024년 불변가격) |
| `home` / `inst` | 재가급여 / 시설급여 | 억원 |
| `recipients` | 급여이용 수급자 | 명 |
| `pop65` | 65세 이상 인구 | 명 |
| `agingRate` | 고령화율 | % |
| `perElder` | 노인 1인당 급여 | **만원** |
| `perRecipient` | 수급자 1인당 급여 | **만원** |
| `coverage` | 수급률 (수급자/노인인구) | % |
| `homeShare` | 재가 비중 | % |
| `severeShare` | 중증 비중 | % |

### 결측 처리 (중요)

- **세종특별자치시는 2012년부터** 존재한다 (2012년 출범). 2010~2011년 행 자체가 없다
- **세종 2012년의 `pop65`, `agingRate`, `perElder`, `coverage`는 `null`** — 인구 자료 미비
- `null`은 0으로 그리지 말고 **점을 건너뛴다**. 지도에서는 회색 처리 + "자료 없음" 툴팁

## 5. 화면 구성

### 5.0 헤더 + KPI 스트립

제목, 한 줄 설명, 그리고 큰 숫자 4개:

| KPI | 값 | 보조 문구 |
|---|---|---|
| 2024년 총급여 | 14.8조 원 | 2010년 대비 4.7배 |
| 노인 1인당 급여 | 전국 평균 | 최고·최저 1.75배 차이 |
| 수급률 | 65세 이상 중 % | 2010년 대비 변화 |
| 재가 비중 | 62.7% | 2012년 49.0%에서 반등 |

> KPI 숫자는 하드코딩하지 말고 `data.json`에서 계산할 것.

### 5.1 섹션 1 — 지역 격차 (Q1)

**차트 3개가 하나의 연도·지표 상태를 공유한다.**

1. **카토그램 지도** — 17개 시도를 `grid` 좌표대로 사각형 격자에 배치, 선택 지표로 색을 칠한다
   - 실제 지도가 아니라 격자임을 캡션에 명시
   - 각 칸에 축약명(`short`)과 값 표시
   - 클릭하면 해당 시도가 "선택 시도"가 되고 섹션 2·3에 반영
2. **순위 막대** — 선택 지표 기준 17개 시도 내림차순. 선택 시도는 강조색
3. **산점도** — X축 고령화율, Y축 선택 지표. **피어슨 상관계수를 화면에 표시**

**컨트롤**
- 지표 토글: `총급여` / `노인 1인당` / `수급자 1인당` / `수급률` / `재가 비중`
- 연도 슬라이더: 2010~2024, 재생(▶) 버튼으로 애니메이션

**필수 주석**: `노인 1인당` 지표를 선택하면 경고 문구를 노출한다 —
> 노인 1인당 지표는 지역 규모를 보정해 비교하기 좋지만, 분모(노인인구)가 지역마다 다른 속도로 변해 왔습니다. 시점 간 비교에는 주의가 필요합니다.

### 5.2 섹션 2 — 시간의 변화 (Q2)

1. **전국 총급여 추이** — 2010~2024 면적 차트. 제도 변화 연도에 세로 마커
   - 2014년: 등급 체계 개편, 4·5등급 신설
   - 2018년: 인지지원등급 신설
   - 마커는 "이 시점에 제도가 바뀌었다"는 **사실 표시일 뿐**이며 효과 주장이 아님을 캡션에 명시
2. **시도별 궤적** — 17개 라인. 기본은 전부 옅은 회색, 선택 시도만 강조
3. **성장 배수 막대** — 시도별 2024÷2010 배수 (세종은 2012 기준, 별도 표기)

### 5.3 섹션 3 — 구조 전환 (Q3)

1. **재가 vs 시설 비중** — `mix` 데이터로 100% 스택 영역 차트
   - 스토리: 57% → 49%(2012 저점) → **62.7%(2024)** V자 반등
2. **등급 구성 변화** — 중증(1~3등급)/경증(4~5등급)/인지지원 100% 스택 영역
   - 중증 100% → 46.8%, 경증 0% → 52.9%
   - 2014년 이전 중증 100%는 **4·5등급이 아직 없었기 때문**임을 캡션에 명시
   - 등급 이름을 모르면 꺾임을 읽을 수 없으므로 차트 아래 **용어 설명(glossary)**을 붙인다
3. **시도별 재가 비중 이동** — 2010 → 2024 덤벨(아령) 차트. 선택 시도 강조

### 5.4 결론 — "그래서 무엇을 알 수 있나"

차트만 늘어놓고 끝내지 않는다. 세 섹션에서 읽어낸 것을 항등식 분해로 정리한다.

1. **커진 것의 대부분은 고령화가 아니다** — `총급여 증가 = 노인인구 증가 × 노인 1인당 급여 증가`
2. **지역 격차는 "얼마를 주나"가 아니라 "누가 받나"에서 온다** — `노인 1인당 = 수급률 × 수급자 1인당`. 로그를 씌우면 곱이 합이 되므로 공분산으로 각 항의 기여도를 나눌 수 있다
3. **제도의 성격이 바뀌었다** — 시설·중증 중심에서 재가·경증 중심으로

**필수**: 결론의 숫자도 `data.json`에서 계산한다. 문장에 박아 넣으면 데이터가 갱신될 때 문장만 옛 숫자로 남는다.

**필수**: 결론 말미에 인과 해석 불가를 명시한다. 셋 다 *무엇이* 일어났는지에 대한 설명이며 *왜*는 이 자료로 답할 수 없다.

### 5.5 한계와 주의사항 (접이식 섹션)

포트폴리오 가점 요소. 다음을 그대로 담는다:

- **인과관계를 주장하지 않습니다.** 제도 변화 시점을 표시했지만 효과를 추정하지 않았습니다
- **수급자 수는 중복배제 값**이라 지역 간 합산이 성립하지 않습니다. 비율 계산에만 썼습니다
- **집계 기준**: 수급자 주민등록주소지 기준(연도말), 전 기간 통일
- **금액은 2024년 불변가격**으로 실질화했습니다 (물가 영향 제거)
- **시군구 단위는 제외**했습니다. 시군구 패널에는 인구·GRDP가 없어 1인당 지표를 만들 수 없습니다
- **세종특별자치시는 2012년 출범** — 이전 데이터가 없고 2012년 인구도 결측입니다

### 5.6 푸터

출처, 기간, 실질화 기준, 생성 방식(`prepare_data.py`), 저장소 링크.

## 6. 기술 스펙

### 스택
- **빌드 없음.** 바닐라 HTML/CSS/JS
- **ECharts 5** (jsDelivr CDN)
- GitHub Pages, `main` 브랜치 루트

### 파일 구성
```
index.html      대시보드 (마크업 + 섹션 구조)
styles.css      스타일
app.js          데이터 로드, 차트 생성, 인터랙션
data.json       데이터 (수정 금지 — prepare_data.py 산출물)
```

### 비기능 요구

| 항목 | 요구 |
|---|---|
| 반응형 | 375px(모바일)~1440px. 모바일에서 가로 스크롤 없을 것 |
| 다크모드 | `prefers-color-scheme` 대응. 차트 색상도 함께 전환 |
| 성능 | 첫 화면 2초 이내. 차트는 뷰포트 진입 시 초기화 |
| 접근성 | 색만으로 정보 전달 금지(값 라벨 병기). 대비 4.5:1 이상 |
| 언어 | 한국어. 숫자는 천단위 구분, 소수 1자리 |
| 오류 처리 | `data.json` 로드 실패 시 안내 메시지 표시 (빈 화면 금지) |

### 디자인 방향

- 데이터가 주인공. 장식 최소화, 여백 충분히
- 색: 파랑 계열 1개를 주색으로, 강조는 주황 1개. 순차 지표는 단색 명도 스케일
- 폰트: 시스템 폰트 스택 (`system-ui`, `Malgun Gothic`)
- 각 차트에 **제목 + 한 줄 해석**을 붙인다. 차트만 던지지 않는다

## 7. 완료 기준

- [x] 세 섹션과 KPI 스트립이 모두 있고 `data.json`에서 값을 계산한다
- [x] 연도 슬라이더·지표 토글·시도 선택이 서로 연동된다
- [x] `null` 값이 0으로 그려지지 않는다 (세종 2012)
- [x] 노인 1인당 지표 선택 시 경고 문구가 뜬다
- [x] 결론 섹션이 있고, 숫자가 실시간 계산된다
- [x] 한계 섹션이 있다
- [x] 375px에서 가로 스크롤이 없다
- [x] 다크모드에서 차트가 읽힌다
- [x] 콘솔 에러 0건
- [x] 지표 5종 × 연도 전 구간에서 값 누수(NaN/undefined) 0건
- [x] `data.json`이 원본 엑셀과 일치한다 (표본 검증)
